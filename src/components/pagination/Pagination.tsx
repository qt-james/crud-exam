import React from "react";
import { Pagination as MuiPagination } from "@mui/material";

const Pagination = () => {
  return <MuiPagination count={1} shape="rounded" />;
};

export default Pagination;
